apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubiya-runner.fullname" . }}-test-connection"
  labels:
    {{- include "kubiya-runner-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubiya-runner.fullname" . }}-agent-manager:{{ .Values.agentManager.service.port }}']
  restartPolicy: Never 